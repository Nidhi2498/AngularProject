import { AccountNameFilterPipe } from './account-name-filter.pipe';

describe('AccountNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
